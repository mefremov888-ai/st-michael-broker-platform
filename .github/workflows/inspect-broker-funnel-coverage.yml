name: Inspect broker funnel coverage (read-only)

on:
  workflow_dispatch: {}

concurrency:
  group: inspect-broker-funnel-coverage
  cancel-in-progress: false

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: SSH and inspect
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script_stop: true
          command_timeout: 10m
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH }}
            git fetch origin master
            git show origin/master:scripts/inspect-broker-funnel-coverage.js > /tmp/inspect-broker-funnel-coverage.js
            docker cp /tmp/inspect-broker-funnel-coverage.js st-michael-api:/app/scripts/inspect-broker-funnel-coverage.js
            docker compose exec -T api node /app/scripts/inspect-broker-funnel-coverage.js
